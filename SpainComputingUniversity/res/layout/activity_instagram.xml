<?xml version="1.0" encoding="utf-8"?><LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <TextView
    android:id="@+id/url_instagram"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:textSize="20sp"
    android:gravity="center_horizontal"/>
    
	<ProgressBar
	    android:id="@+id/progressBarInstagram"
	    style="?android:attr/progressBarStyleLarge"
	    android:layout_width="wrap_content"
	    android:layout_height="match_parent"
	    android:layout_gravity="center_vertical|center_horizontal"
	    android:visibility="gone" />
    
	<GridView 
	    android:id="@+id/gridInstagram"
	    android:visibility="gone"
	    android:layout_width="match_parent" 
	    android:layout_height="wrap_content"
	    android:numColumns="2"/>
	
	<ImageView
	android:id="@+id/instagramImageView"
	android:contentDescription="@string/InfoAplicacion"
	android:background="@drawable/university"
	android:visibility="gone"
	android:layout_gravity = "center"
	android:layout_width="225dip" 
	android:layout_height="225dip"/>
	
</LinearLayout>